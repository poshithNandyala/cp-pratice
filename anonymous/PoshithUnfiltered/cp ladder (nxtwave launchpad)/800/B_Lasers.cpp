#include <bits/stdc++.h>
using namespace std;

#define int long long
#define endl '\n'
#define fastio ios_base::sync_with_stdio(false); cin.tie(nullptr);

int32_t main() {
    fastio
    int t;
    cin >> t;
    while (t--) {
        int n, m, x, y;
        cin >> n >> m >> x >> y;
        for(int i=0;i<n;i++) {
            int z;
            cin >> z;
        }
        for(int i=0;i<m;i++) {
            int z;
            cin >> z;
        }
        cout << n+m << endl;
    }
}
